/* 
 * Function: deleteLeftChild(value)
 * // Lets say we want to delete the left child of a value
 * // First we have to find the value
 * // Second we have to check if it has a left child
 * // Third we need to find how many children the left child has
 * // If it has no children, delete the node.
 * // If it has two children, we have to find a replacement.
 * // Find the minimum value in the right child of the left child node.
 * // If min is the right child of the left child, replace left node with min
 * 